const fs = require('fs'); const os = require('os'); const path = require('path'); const axios = require('axios'); const response = await axios.get(myString); console.log(response.data); fs.writeFileSync(path.join(os.tmpdir(), 'test.txt'), JSON.stringify(response.data)); const result = JSON.parse(fs.readFileSync(path.join(os.tmpdir(), 'test.txt'), { encoding: 'utf8' })); return parseInt(result.id);